version: '3.8'

services:
  fastapi-app:
    build: .
    ports:
      - "80:80"  # Map port 80 inside the container to port 80 on Azure
    environment:
      - PYTHONUNBUFFERED=1
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "80"]  # Ensure it's listening on port 80
